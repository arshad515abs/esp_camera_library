# ESP Camera Library

A simplified Arduino wrapper for the official ESP32 `esp_camera` driver.

## Installation
1. Download this repo as ZIP
2. In Arduino IDE: Sketch > Include Library > Add .ZIP Library

## Usage
See `examples/CameraTest/CameraTest.ino`

**Note**: Requires the official ESP32 Arduino core (esp_camera is included in it).